{
  "private": true,
  "scripts": {
    "dev": "webpack-dev-server --mode development --open",
    "build": "rimraf pkg dist && webpack --mode production",
    "build:dev": "rimraf pkg dist && webpack --mode development",
    "test": "wasm-pack test --chrome --firefox --safari --headless"
  },
  "devDependencies": {
    "@wasm-tool/wasm-pack-plugin": "^1.6.0",
    "copy-webpack-plugin": "^11.0.0",
    "css-loader": "^6.7.0",
    "sass": "^1.55.0",
    "sass-loader": "^13.0.2",
    "style-loader": "^3.3.0",
    "wasm-pack": "^0.10.1",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0",
    "webpack-dev-server": "^4.11.0"
  }
}
